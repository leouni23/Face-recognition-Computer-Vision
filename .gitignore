# Environment
.env
*.env

# Virtual environment
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# Claude Code
.claude/

# IDE
.vscode/
.idea/
*.swp

# macOS
.DS_Store

# Logs
*.log

# Database local files
*.db
*.sqlite3

# Planimetrie caricate dall'utente (possono rappresentare ambienti reali — mai pubblicarle)
web/static/maps/

# Artefatti runtime persistenti (DB, validation, benchmark, log) — montati come volume /data
data/

# Jetson deploy secrets/config
.env.jetson

# ── Dati di validazione / biometrici: MAI su un repo pubblico ────────────────────
# data/ è già ignorata, ma la root della revisione offline è scegliibile dall'operatore
# e potrebbe cadere dentro il working tree: queste regole valgono ovunque nel repo.
validation/
# export "Concludi validazione" (metriche + report della campagna)
export/
campaign.json
review_state.json
.active_campaign.json
session.json
# detections.jsonl / labels.jsonl / runs.jsonl
*.jsonl
PROTOCOL.md

# Media registrati (volti riconoscibili) e modelli/embedding
*.mp4
*.avi
*.mkv
*.mov
*.onnx
*.npy
*.pkl

# Cache/stato locale della revisione offline
.cache/
faceid-review.json
